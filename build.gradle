plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

jar {
    manifest {
        attributes(
                'Main-Class': 'Main'
        )
    }
}

dependencies {
    // Something
    testCompile group: 'junit', name: 'junit', version: '4.12'
    compile files('libs/Pokemon.jar')
}
